import projetos from "./data.js"; document.addEventListener("DOMContentLoaded", (() => { const t = new URLSearchParams(window.location.search), e = parseInt(t.get("id")), o = projetos.find((t => t.id === e)); document.title = `${o.titulo} | maribe arquitetura`; document.getElementById("project-info").innerHTML = `\n        <h1>${o.titulo}</h1>\n        <h2>${o.cidade}, ${o.ano}</h2>\n        <p>${o.descricao}</p>\n    `; const n = document.getElementById("project-pictures"); o.outrasFotos.forEach((t => { const e = document.createElement("div"), a = document.createElement("a"); a.href = t, a.setAttribute("data-lightbox", "fotos"), a.setAttribute("data-title", `${o.titulo}`); const d = document.createElement("img"); d.src = t, d.alt = "Imagem de projeto da Maribe Arquitetura", d.title = `${o.titulo}`, a.appendChild(d), e.appendChild(a), n.appendChild(e) })) }));